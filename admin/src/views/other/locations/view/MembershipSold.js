// ** React Imports
import { Fragment } from 'react';

// ** Reactstrap Imports
import { Card, CardBody, CardTitle, CardHeader } from 'reactstrap';

const MembershipSold = () => {
  return (
    <Fragment>
      <Card>
        <CardHeader>
          <CardTitle tag="h4">Memberships Details</CardTitle>
        </CardHeader>
        <CardBody>
          <span>Membership code is not working, thus unable to implement 😔!</span>
        </CardBody>
      </Card>
    </Fragment>
  );
};

export default MembershipSold;
